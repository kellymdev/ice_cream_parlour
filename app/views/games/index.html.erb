<h2>Games</h2>

<%= link_to 'New Game', new_game_path %>

<table>
  <tr>
    <th>Name</th>
    <th>Balance ($)</th>
    <th>Profit/Loss ($)</th>
    <th></th>
  </tr>

  <%= render partial: 'game', collection: @games %>
</table>
